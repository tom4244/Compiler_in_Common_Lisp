$$
$$
    int  a,b,c,i,j,max,sum;
    boolean signal;

  
    if (a = 3) signal := false; endif


    if (a < b) a := c; endif

    max := 3;
    sum := 0;
    i := 1 * ((max - 2) - sum);
    j := (1 * 3)*(sum + (sum*3) - i * 2);
    signal := true;
    read( max, signal);
    while (i < sum)  {
          sum := sum + i;
          i  := -i + 1; 
     }
     write (sum+max);
$$